/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts.bak

# env
.env

# database
/prisma/*.db
/prisma/*.db-journal

# Prisma 生成 client（`npx prisma generate` で再生成）
/generated

# 前回成果のスナップショット（読者は生成しない、gitで履歴管理する対象外）
/0_before_regen_snapshot

# .claude/context の作業一時ファイル
/.context

# misc
.DS_Store
